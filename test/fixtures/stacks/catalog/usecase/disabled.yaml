components:
  terraform:
    cloudwatch-logs/disabled:
      metadata:
        component: target
      vars:
        enabled: false
        name: cloudwatch-logs
        retention_in_days: 30
        stream_names:
          - app-1
          - app-2

